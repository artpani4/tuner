import { ITunerConfig } from './type.ts';

/**
 * Возращает объект типа ITunerConfig
 * @param cfg Объект конфигурации
 * @returns Объект типа ITunerConfig
 */
export default function tune(cfg: ITunerConfig) {
  return cfg;
}
